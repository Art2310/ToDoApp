package main;

import controller.ProjectController;
import controller.TaskController;
import java.sql.Connection;
import java.util.Date;
import java.util.List;
import model.Project;
import model.Task;
import util.ConnectionFactory;

public class Main {
    public static void main(String[] args) {
        
        /*
        ProjectController projectController = new ProjectController();;
        
        Project project = new Project();;
        project.setName("Teste");
        project.setDescription("desc");
        projectController.save(project);
        
        project.setId(2);
        project.setName("new name");
        projectController.update(project);
        List<Project> projects = projectController.getAll();
        System.out.println("Total of projects: "+ projects.size());
        
        projectController.removeById(1);
        
        TaskController taskController = new TaskController();;
        
        taskController.removeById(1);
        
        Task task = new Task();
        task.setIdProject(1);
        task.setName("Teste1");
        task.setDescription("desc 1");
        task.setNotes("Note 1");
        task.setIsCompleted(false);
        task.setDeadline(new Date());
        
        taskController.save(task);
        task.setId(2);
        task.setName("new name1");
        taskController.update(task);
        List<Task> tasks = taskController.getAll(1);
        System.out.println("Total of tasks: "+ tasks.size());*/
        
        
    }
}

